#image: karbon001/gitlab-ci-laravel-php
image: kahunacoder/docker-laravel-gulp
  
before_script:
  # - php -v
  # - composer --version
  # - ls -lah
  # - composer install --no-interaction
  # - php artisan env
  # - npm install
  # - bower install
  # - php artisan migrate --seed

build:
  script:
    - gulp build