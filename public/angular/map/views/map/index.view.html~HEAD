<section ng-controller="ContactController">
  <div class="col-lg-2">
    <section ng-include="'./angular/lead/views/side-menu.view.html'"></section>
  </div>
  <div class="col-lg-10">
    <section ng-include="'./angular/lead/views/search-bar.view.html'"></section>
    <table datatable="ng" class="row-border hover" ng-init="find()">
      <thead>
          <tr>
              <th>#</th>
              <th>Name</th>
              <th>Phone</th>
              <th>Email</th>
              <th></th>
          </tr>
      </thead>
      <tbody>
          <tr ng-repeat="contact in contacts" ng-click="findOne(contact.id)">
              <td>{{ contact.id }}</td>
              <td>{{ contact.name }}</td>
              <td>{{ contact.phone }}</td>
              <td>{{ contact.email }}</td>
              <td>
                <a href="#" ng-click="deactivate(contact)"><i class="glyphicon glyphicon-trash"></i></a>
              </td>
          </tr>
      </tbody>
    </table>
  </div>
</section>